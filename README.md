# lwcc

London Women Chess Club static website.

## Preview

Open `index.html` in a browser.

## Structure

- `index.html` and other `.html` pages live in the repo root
- `css/styles.css` contains the shared styles
- `images/` holds local image assets
